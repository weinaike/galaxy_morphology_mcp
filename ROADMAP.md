# 项目路线图

## 当前阶段

成分分析重构：阶段 A～M 和 N0～N5 已完成，JWST0831 对象 `104` 的 v5 工程 canary 已真实执行 promotion／add candidate 和 refit 评价，但该 run 的 VLM 被关闭，且 rejected candidate lifecycle 的返回元数据污染导致 renderer 漏报评价字段；Image 仍为 `FIT_AVAILABLE`、`UNLOCKED`。下一阶段以 `docs/component-analysis/workflow-redesign-execution-plan.md` 第 13～16 节的 N7～N11 为唯一执行入口，先修复可观测性并重跑仅 `104` 的 VLM canary；确认前不运行 `1071`、`1118` 或其余 31 个对象。JWST0716 v2 历史结果保持只读；Edge-on Disk 不参与本次优化；现有 MCP 拟合工具仍是唯一实际执行入口。

## 已完成

- 已确认保留数值证据层、VLM 形态证据层、确定性规则与迭代控制层三层架构。
- 已确认正式成分白名单为 Disk、Bulge、Edge-on Disk、Bar、AGN、Fourier m=1、Companion 七类（2026-08-13 更新：Lens 保留并升入白名单，现为八类）。
- 已确认 AGN 与 `compact central source candidate` 的物理身份分开。
- 已确认 GalfitS Disk 使用 `sersic` 且确认后固定 `n=1`。
- 已确认中心成分采用严格同心审计规则。
- 已确认尺寸层级作为通常物理期望和复核依据，不作为无条件硬失败。
- 已确认可选成分默认使用 `BIC_gain >= 10` 统计门槛，并且必须同时通过残差、拟合、参数和物理证据。
- 已形成 `docs/component-analysis/redesign.md` 与 `docs/component-analysis/spec-sync.md`。
- 2026-08-13：四项开放判据（Disk／Elliptical 最低证据组合、Bar 单高质量波段触发、中心源候选 P 块小 Re Sérsic、Bulge／中心源可分辨尺度门）已裁定为 v1 默认规则，写入 `docs/component-analysis/redesign.md`（升至 v4），阈值待评测校准。
- 2026-08-13：四类 artifact schema（artifact_manifest、numeric_evidence、vlm_evidence、decision_artifact）已冻结 v1.0，位于 `src/schemas/`，含加载与校验入口；`tests/test_schemas.py` 39 个用例全部通过。
- 2026-08-13：实施顺序第 3 步完成：`src/component_analysis/` 实现数值证据层（numeric.py：加权二阶矩、方位 Fourier 模、FWHM 反卷积、孔径 SNR、mask-aware 局部峰检测、numeric_evidence 组装）与纯规则层（rules.py：七类成分 v1 提议规则、重拟合仲裁、BIC 门、阈值版本化），输出均经冻结 schema 校验；未接管现有 workflow。实现由另一 agent 起草，本轮审查后修复 Disk 提议规则中 `and/or` 优先级错误（VLM 支持盘时 N2/N3 应二选一，原写法忽略 N3 路径）；`tests/test_component_analysis.py` 43 个用例全部通过。
- 2026-08-13：完成旧残差分析 prompt 经验条目化盘点（`docs/component-analysis/prompt-experience-inventory.md`，第 4 步子任务 1）：逐条标注去向（RULES／NUMERIC／VLM_LABEL／SPEC／STAY／DROP／REPLACED）。用户裁定：非建模形态（旋臂／壳层／团块等）的识别描述不迁移，仅保留受控标签枚举；调参层量化门（sky fix、伴星系 20px／5 星等门等）留在现有 prompt 不迁移。两项 OPEN（Lens 认定、Nucleus 代偿 Bulge）待规范同步前裁定。
- 2026-08-13：两项 OPEN 裁定落实：Lens 保留并升入正式白名单（八类，按可选成分 `BIC_gain >= 10` 门管理）；「Nucleus 代偿 Bulge、物理意义优先于奥卡姆」废弃，中心源统一走可分辨尺度门分流。`docs/component-analysis/redesign.md` 升至 v5（第六节新增第 8 小节 Lens 判据：Bar 参数异常＋延展区正残差触发，接受条件 `Re_disk > Re_lens > Re_bar`、`n_lens < 0.5`、`q_lens > 0.5`），`docs/component-analysis/spec-sync.md` 与 `docs/component-analysis/prompt-experience-inventory.md` 已同步；`decision_artifact` schema 的 component 枚举增补 `lens`（向后兼容的增量修订，仍为 1.0），`rules.py` 新增 `_lens_rule`（阈值 `re_bar/re_disk >= 0.9` 或 `q_bar > 0.5`，属 v1 默认值待校准）并把 lens 纳入可选成分 BIC 门。
- 2026-08-13：整理项目文档结构：成分分析方案归档至 `docs/component-analysis/`，工程优化清单归档至 `docs/engineering/`，专利材料归档至本地忽略目录 `docs/patent/`；`ROADMAP.md` 保留在仓库根目录作为进度入口。
- 2026-08-13：`INCONCLUSIVE` 自动化消解策略落实（用户确认拟合流程须全自动、无人工阻塞）：`docs/component-analysis/redesign.md` 升至 v6，第四节新增三类消解——试拟合仲裁（降级为弱候选交 `EVALUATE_REFIT` 自动裁决）、保守默认兜底（不改变成分结构）、VLM 断供纯数值降级；试探预算默认 3 次、同一 rule ID 证据未变二次 `INCONCLUSIVE` 即终结、被否候选不重提；人工复核改为事后批量审查 `needs_review` 标志。`decision_artifact` schema 增补可选 `automation` 块（policy 版本、原动作、消解方式、理由、`needs_review`，向后兼容的增量修订仍为 1.0）；新增 `src/component_analysis/policy.py`（automation-policy@v1，不改动规则函数，只包装其输出）；`spec-sync.md` 同步 workflow 行、BIC 表与验收标准。
- 2026-08-17：实施顺序第 4 步完成：新增 `src/component_analysis/vlm.py`，从冻结 schema 派生标签与质量标志枚举，只向 VLM 暴露数值层签发的 `target_id`，不暴露候选坐标；提供版本化受控 prompt、严格 JSON parser、语义冲突检查及 `PARSE_FAILED`／`TIMEOUT`／`REFUSED` 空证据降级。旧 prompt 经验按盘点裁定仅保留受控标签，不迁移识别指南、坐标或动作决策；现有 workflow、残差分析 prompt 和 verifier 均未修改。
- 2026-08-17：新增 `src/component_analysis/artifact_adapter.py`，从显式 round／lyric／gssummary 输入构建 manifest，逐波段校验 result HDU 布局、science／mask shape、WCS、单位和有限值比例，并加载 residual／mask／sigma／model／original 与 PSF；不通过 glob 猜测输入。
- 2026-08-17：新增 `src/component_analysis/shadow.py` 与 `docs/component-analysis/shadow-mode.md`。runner 独立写出 manifest、numeric evidence、受控 VLM prompt／raw response、VLM evidence 和 decision artifact；VLM 通过 callback 隔离，未配置时明确记为 `REFUSED` 并走纯数值降级，不执行拟合或写回 workflow。
- 2026-08-17：真实 JWST PSF 验证发现整幅二阶矩受衍射翼主导，新增中心核半高宽测量，将 `obj_170` 七波段 PSF FWHM 从错误的 `18–31 px` 修正为 `2.65–4.00 px`；局部峰 ID 改为波段限定，消除多波段 `candidate_1` 冲突。
- 2026-08-17：新增 `src/component_analysis/derived.py`，从真实 FITS 派生源尺度／PSF 比、外层等照度几何、外层轴比、连续同号径向残差、中心过量与逐波段分辨尺度、中心 m=2／拉长、原图等照度 m=1、Bar profile 和原图候选对应源；从 gssummary 派生单 Sérsic n 与 Bar／Disk 尺度关系。数值层只输出测量事实和质量状态，不输出成分或动作。
- 2026-08-17：局部残差峰通过 science WCS 合并为稳定的跨波段候选 ID，记录检测波段与位置散布；`obj_170` 的 34 个波段内检测合并为 13 个候选，其中 10 个具有多波段对应。
- 2026-08-17：修正 Bar PSF 否决语义：尺度门通过不能冒充完整衍射检查；`psf_veto` 改为 `true | false | null`，规则层仅接受显式 `false` 的强等照度证据。实际 PSF／V3 方向相关性尚未实现。
- 2026-08-18：完成 PSF 模板方向相关性检查：`measure_directional_harmonic_alignment` 在候选 Bar 尺度环带比较实际卷积 PSF 与原图／残差的 `m=6` 轴方向，允许残差正负号翻转；质量不足保持 `null`，并记录方向来源、相位差、谐波 SNR、覆盖率及可用的 `PA_V3` 元数据。`obj_170` 中 F410M、F277W 命中衍射方向否决，F115W、F150W 因 SNR 不足保持 `null`；没有波段提供 Bar 的显式通过证据。
- 2026-08-18：新增 `OpenAICompatibleVLM`，读取根目录 `.env` 的 `OPENAI_API_KEY`、`OPENAI_BASE_URL` 和 `OPENAI_MODEL`，发送 comparison PNG 与受控 prompt，要求 OpenAI-compatible `response_format=json_object`，并把实际 `model_id` 写入 VLM evidence；不修改共享旧 provider 或现有 workflow。
- 2026-08-18：`obj_170` 最优轮完整 shadow artifact 已落盘。数值层产出 77 项特征，34 个波段内峰合并为 13 个 WCS 候选；retry1 的 VLM 返回 `central: disk_like` 与 `central: spiral_arm`，严格 JSON 解析通过；规则层输出 `KEEP_AND_CONTINUE`，`needs_review=true`，不改变当前 Disk＋Bulge 结构。旧方案第一次调用因过期 key 记录在前一目录，更新 key 后的结果位于 `/home/www/2026/GALFITS_examples/jwst0716/170/shadow/20260818_obj170_iter5_component_analysis_v1_retry1`。
- 2026-08-19：将科学家提供的 JWST0716 多波段最终拟合标签结构化为 `docs/component-analysis/expert-final-labels.json`，作为 shadow run 的终态成分对照基准。评测范围共 32 个已确认对象；`obj_324` 因 F277W mask 覆盖过大、`obj_137` 因缺少专家标签，均移出基准。按专家定义，`nucleus` 规范化为 AGN，`single sersic` 规范化为 Disk，`edge-on disk` 保持独立；`disk(lop)` 规范化记录为 `disk`＋`fourier_m1`。
- 2026-08-20：建立 `docs/component-analysis/shadow-dev-set-jwst0716.json`，固定 20 个 JWST0716 shadow 输入对象及其历史轮次。清单覆盖全部 14 种语义终态成分组合，并为 Edge-on Disk、Disk＋Bulge、Disk＋Fourier m=1＋Bulge、Disk＋Fourier m=1＋Bar、Disk 与 Disk＋Fourier m=1＋AGN 增加稳定性重复样本。全部 20 轮均经 artifact adapter 实测通过；`obj_317` 为 6 波段、`obj_1831` 为 5 波段，其余为 7 波段。所选轮次仅是完整的历史输入，不视为专家终态真值。
- 2026-08-20：将初版 20 对象清单标记为 superseded，新增轮次级清单 `docs/component-analysis/shadow-dev-set-jwst0716-rounds.json`。该清单纳入 116 个完整非 SED 历史轮次：29 个与专家终态成分完全一致、26 个缺少一个终态成分、24 个缺少多个终态成分；另按对象保留 15 个纯额外成分和 22 个混合差异代表轮次。每轮均记录 lyric／gssummary／comparison PNG、语义成分差异、BIC 和 reduced chi-square，并通过 artifact adapter 输入校验。
- 2026-08-20：实现历史 lyric 成分语义规范化：`obj0`／`obj1` 优先按 profile 类型和相邻注释解析，`nucleus`→`agn`、`edgeondisk`→`edge_on_disk`，单一无注释 `obj0` Sérsic 才回退为 `disk`，`sersic_f` 的 `P?21) 1` 记录 `fourier_m1`；无法解析的泛化名称直接失败。批量入口强制校验规范化结果与清单 `source_components` 一致。新增 `tests/test_shadow.py`，与 adapter／规则层回归共 61 项通过。
- 2026-08-20：对轮次级清单完成 116/116 numeric-only shadow，未调用 VLM、未执行 GALFIT、失败 0。规则动作为 `PROPOSE_ADD` 67 轮、`KEEP_AND_CONTINUE` 49 轮；摘要位于 `docs/component-analysis/shadow-dev-results-jwst0716.json`，逐行对照表位于 `docs/component-analysis/shadow-dev-review-table-jwst0716.tsv`，完整临时 artifact 位于 `/tmp/jwst0716-shadow-rounds-20260820-parallel`。拟议成分中 22 轮属于专家终态、45 轮不属于专家终态，后者仅是人工复核优先级，不直接判定规则错误。
- 2026-08-21：完成 JWST0716 完整 VLM shadow 的第一阶段人工复核：逐轮审计 19 个 `PARSE_FAILED` raw response，归因为 7 个中心标签互斥冲突、7 个响应中途截断、4 个 `evidence_regions` 契约不匹配和 1 个低质量标签违规；完成 116 轮动作方向、逐成分覆盖、VLM／numeric-only 差异和对象级重复性统计，并对 16 个代表轮次完成 numeric evidence、VLM evidence、rule trace、decision artifact 与专家终态标签的结构化预审。总结报告位于 `docs/component-analysis/shadow-dev-evaluation-report-jwst0716-vlm.md`，结构化结果位于 `docs/component-analysis/shadow-dev-manual-review-jwst0716-vlm.json`。按用户指示，不再继续这 16 个轮次的 comparison PNG 视觉复核；该项不作为阻塞或待办。
- 2026-08-21：落实人工复核后不涉及科学裁决的首项修复：`docs/component-analysis/redesign.md` 升至 v8，明确 `evidence_regions` 契约和 Edge-on Disk 暂缓范围；VLM prompt 升至 `component-analysis-vlm@v1.1`，明确每个证据区域必须使用 `band:panel:region_id`、提供合法示例并允许无法定位时返回空数组。parser、schema、规则阈值和 workflow 均未改动。候选 ID 图像映射、`central` 多尺度语义和 `diffraction_psf` 的定位／证据优先级已记录为待用户或科学家裁定项。
- 2026-08-24：落实用户裁定的两项 VLM 契约修改。候选位置使用 `src/component_analysis/candidate_overlay.py` 生成 `candidate_overlay.png`，由数值层在各波段 original/residual 面板绘制 `candidate_N`；shadow runner 将 overlay 传给 VLM，原始 comparison PNG 保留不变，坐标不进入 prompt。中心语义采用方案 B：新 prompt 升至 `component-analysis-vlm@v1.2`，不暴露历史 `spheroid_like`；新 parser 拒绝该旧标签，冻结 schema 仍允许历史 JSON 校验，`_disk_rule` 与单 Sérsic 光球分流均不再读取该标签。新增科学家讨论入口 `docs/component-analysis/jwst0716-vlm-scientist-discussion-brief.md`，Edge-on Disk 继续排除在优化范围外。

## 进行中

- 除 Edge-on Disk 外，逐成分 v1 默认判据已裁定并先行实施；阈值（N1/N2/N3 组合、`FWHM_int >= 0.5×FWHM_psf`、SNR 门等）待 dev set 和 held-out 评测后校准优化。Edge-on Disk 保留现有实现，但在科学定义和建模边界统一前不参与优化。
- shadow mode 的单轮真实 VLM 验证、116 轮历史 numeric-only／完整 VLM v2 及 v3 proposal shadow 已完成；候选映射、中心语义、决策逻辑、对象级 PolicyState、v1.1 action contract 和结构化 workflow bridge 已实施。N0～N5 和 104 v5 工程 canary 已完成；N7～N11 的 lifecycle、provider、renderer 和 VLM-enabled 104 canary 待执行，正式默认 workflow 仍未接管。

## 待办

- 按 `docs/component-analysis/workflow-redesign-execution-plan.md` 第 13～16 节顺序完成 N7～N11：先修复 rejected candidate lifecycle schema 污染和 renderer 漏报，再运行测试、v5 离线 replay 和全量回归。
- N7～N8 通过后，只为 `104` 创建新的 VLM-enabled run；提交报告并等待小鱼儿确认前，不运行 `1071`、`1118` 或其余 31 个对象。
- 基于 v2 proposal shadow 按 `object_id` 划分 held-out 历史轮次，保证同一对象不跨校准集与测试集；按对象或完整拟合序列汇总泛化结果，并检查不同 PSF、mask 和残差质量下的三态稳定性。
- 按 `jwst0716-vlm-scientist-discussion-brief.md` 第七节保留 `diffraction_psf` 的当前科学规范；本轮不新增阈值校准或全局否决逻辑。
- Edge-on Disk 的规则、prompt 和阈值优化暂缓；待科学定义和建模边界统一后单独建立评测。
- 将可选 catalog 接入候选正向佐证；未匹配仍不得直接否决候选。
- 科学家讨论前先阅读 `docs/component-analysis/jwst0716-vlm-scientist-discussion-brief.md`；需要逐轮细节时再查阅报告、review JSON 和 `/tmp/jwst0716-shadow-rounds-20260820-vlm` 三个 diffraction 案例目录。
- 建立 held-out 评测集，校准阈值并进行科学家评审。
- 在真实局部残差事实和 A/B remove pilot 完成前，`PROPOSE_REMOVE` 保持 review-only；完成 20～30 个对象／轮次的 A/B refit 和科学家后验判断后，再评估是否扩大启用范围。
- 通过真实 pilot 的 verifier PASS 和科学家评审后，再申请让新规则层接管正式动作决策。
- 只有新的 104 VLM canary 完成 Image `CONVERGED`、verifier `PASS`／`lockable=true` 和 best round `LOCKED`，并经小鱼儿确认后，才允许分别新建 `1071`、`1118` run；其余 31 个对象不在当前授权范围。

## 阻塞

- `PROPOSE_REMOVE` 所需的真实局部残差事实、成分映射和 A/B remove pilot 尚未完成，自动删除保持关闭。
- Edge-on Disk 科学定义和建模边界尚未统一；该成分已移出当前优化范围，不阻塞其他成分评测。
- v1 判据阈值属默认值，待评测校准，不阻塞实施。
- v5 rejected candidate lifecycle 含 schema 禁止的 `lifecycle_file`、`fit_valid`，导致 renderer 拒绝读取 refit evaluation；N7 修复和回归通过前，阻塞新的真实 canary。
- v5 的 `use_vlm=false`，不能作为科学 workflow 验收样本；新的 104 canary 必须显式启用 VLM 并记录 provider／timing 状态。

## 最近验证

- 2026-08-13：完成现有规范与重构方案的只读对照，并更新重构方案和规范同步方案；现有规范同步与代码实现尚未执行。
- 2026-08-13：`/home/www/ENTER/bin/python -m pytest tests/test_schemas.py` 39 passed（pytest 9.1.1 已经确认后安装到 ENTER 环境），四类 schema 的合法样例、非法样例和层边界禁令均验证通过。
- 2026-08-13：`pytest tests/test_component_analysis.py tests/test_schemas.py` 82 passed（新增 43 个）：数值函数用已知矩／m=1／m=2／FWHM 的合成图像验证，规则层用纯 JSON fixture 覆盖七类成分的 PROPOSE／ACCEPT／REJECT／INCONCLUSIVE 分支（含 Bar 单高质量波段触发、中心源 resolved／unresolved／AGN 分流、VLM 解析失败降级、可选成分 BIC 门）。
- 2026-08-13：Lens 落实后全量回归 `pytest tests/test_component_analysis.py tests/test_schemas.py` 88 passed（新增 6 个 Lens 用例：Bar 参数异常＋延展残差提议、缺延展残差 INCONCLUSIVE、高置信 independent_source 否决、无 Bar 不提议、正常 Bar 参数不触发、lens 低 `BIC_gain` 拒绝）。
- 2026-08-13：policy 层落实后全量回归 `pytest tests/test_component_analysis.py tests/test_schemas.py tests/test_policy.py` 103 passed（新增 15 个 policy 用例：四类试拟合降级（Disk 模糊证据、Edge-on 替换、中心源分辨冲突取 Bulge 路径、Companion 带 target label）、衍射冲突保守兜底、预算耗尽、被否候选不重提、重复 `INCONCLUSIVE` 终结、VLM 断供纯数值降级两路径、`EVALUATE_REFIT` 兜底拒绝与状态记录、非 `INCONCLUSIVE` 直通）。
- 2026-08-17：`/home/www/ENTER/bin/python -m pytest tests/test_vlm.py tests/test_component_analysis.py tests/test_schemas.py tests/test_policy.py` 124 passed（新增 21 个 VLM 用例，覆盖 prompt 边界、合法 JSON、未知标签、缺字段、坐标字段、越权目标、标签冲突、低质量约束、严格 JSON、超时与拒答降级）；存在 1 条既有 pytest 配置警告：当前环境未识别 `asyncio_mode`。
- 2026-08-17：`/home/www/ENTER/envs/galfit/bin/python -m pytest tests/test_component_analysis.py tests/test_artifact_adapter.py tests/test_vlm.py tests/test_schemas.py tests/test_policy.py` 129 passed；新增 adapter／shadow runner、PSF 核半高宽及跨波段候选 ID 测试。仍有同一条既有 `asyncio_mode` 配置警告。
- 2026-08-17：只读运行 `/home/www/2026/GALFITS_examples/jwst0716/170/output/20260717_161120_obj_170_iter5`：7 个波段的路径、5-HDU result 布局、shape、WCS、单位和有限值检查全部通过；数值层产出 42 项基础特征，VLM 未调用而记录 `REFUSED`，decision 为带 `needs_review` 的 `KEEP_AND_CONTINUE`。产物位于 `/tmp/obj170-shadow-8xAnFG`，该结论仅验证链路，不作为完整科学判断。
- 2026-08-17：`/home/www/ENTER/envs/galfit/bin/python -m pytest tests/test_derived_evidence.py tests/test_artifact_adapter.py tests/test_component_analysis.py tests/test_vlm.py tests/test_schemas.py tests/test_policy.py`：134 passed；存在同一条既有 `asyncio_mode` 配置警告。
- 2026-08-17：对 `obj_170` 最优轮运行完整派生层，产出 77 项特征；34 个波段内局部峰合并为 13 个 WCS 候选。初次运行暴露 Bar 的 `psf_veto=false` 语义错误并与旧报告的衍射芒否决冲突；修正三态门后，用同一真实数值证据重跑规则层得到带 `needs_review` 的 `KEEP_AND_CONTINUE`，trace 为 `FOURIER_M1_CONFOUNDING_V1`。完整原始产物位于 `/tmp/obj170-shadow-derived-zXerZm`，其中 decision 生成于三态修正前，仅保留作问题复现，不代表修正后的最终动作。
- 2026-08-18：`/home/www/ENTER/envs/galfit/bin/python -m pytest tests/test_component_analysis.py tests/test_derived_evidence.py tests/test_artifact_adapter.py tests/test_vlm.py tests/test_vlm_provider.py -q`：85 passed；存在同一条既有 `asyncio_mode` 配置警告。
- 2026-08-18：`obj_170` retry1 shadow 运行完成并通过四类 schema 校验；VLM `parse_status=OK`、model `gemini-3.1-pro-preview`、2 条受控观察；Bar trace 为 `BAR_EVIDENCE_V1: NOT_SATISFIED`，决策为 `KEEP_AND_CONTINUE`，`needs_review=true`。旧新手工对照摘要与 artifact 同目录保存。
- 2026-08-18：全量 `pytest -q`：160 passed、1 skipped、15 failed。失败集中在未修改的旧集成边界：缺少可选 `claude-agent-sdk`（4 项）、`create_comparison_png` 当前 tuple 返回值与旧测试字符串断言不一致（10 项）、`run_galfits` 旧日志断言（1 项）；本次成分分析聚焦套件仍为 85 passed。
- 2026-08-18：最终成分分析回归 `/home/www/ENTER/envs/galfit/bin/python -m pytest tests/test_component_analysis.py tests/test_derived_evidence.py tests/test_artifact_adapter.py tests/test_vlm.py tests/test_vlm_provider.py tests/test_schemas.py tests/test_policy.py -q`：139 passed；存在同一条既有 `asyncio_mode` 配置警告。
- 2026-08-20：`/home/www/ENTER/envs/galfit/bin/python -m pytest tests/test_shadow.py tests/test_artifact_adapter.py tests/test_component_analysis.py tests/test_derived_evidence.py tests/test_schemas.py -q`：104 passed；存在同一条既有 `asyncio_mode` 配置警告。116 轮 numeric-only shadow 全部完成，FITS WCS 的 `FITSFixedWarning` 为 astropy 对历史时间／观测站字段的自动修正提示。
- 2026-08-20：单轮真实 VLM smoke 使用 gemini-3.1-pro-preview 返回 parse_status=OK，并由 independent_source 观察参与规则决策；随后 116 轮完整 VLM shadow 全部完成，97 轮 parse_status=OK、19 轮 PARSE_FAILED、runner failures=0。最终动作 48 轮 PROPOSE_ADD、68 轮 KEEP_AND_CONTINUE；四类 artifact 全量通过 schema。结果位于 docs/component-analysis/shadow-dev-results-jwst0716-vlm.json，与 numeric-only 基线相比 21 轮动作发生变化。
- 2026-08-21：人工复核 JSON 通过标准库解析；19 个失败条目、16 个代表轮条目及唯一 ID 数量断言通过；失败轮次 ID 与 116 轮源汇总逐项一致，动作计数和 proposal 方向计数重新计算一致。总结 Markdown、结构化 JSON 和 `ROADMAP.md` 通过局部空白与差异检查；本阶段按用户指示不包含 comparison PNG 视觉验证。
- 2026-08-21：VLM prompt v1.1 修复后运行 `/home/www/ENTER/envs/galfit/bin/python -m pytest tests/test_component_analysis.py tests/test_derived_evidence.py tests/test_artifact_adapter.py tests/test_vlm.py tests/test_vlm_provider.py tests/test_schemas.py tests/test_policy.py tests/test_shadow.py -q`：143 passed；存在同一条既有 `asyncio_mode` 配置警告。结构化 review JSON 已通过标准库解析，并确认包含 3 项待裁决记录和 16 个代表轮条目。
- 2026-08-24：运行 `/home/www/ENTER/envs/galfit/bin/python -m pytest tests/test_vlm.py tests/test_component_analysis.py tests/test_artifact_adapter.py tests/test_schemas.py tests/test_shadow.py -q`：122 passed，存在同一条既有 `asyncio_mode` 配置警告。新增测试覆盖 candidate overlay artifact、VLM 输入路径、方案 B 数值-only 中心分流和旧 `spheroid_like` 新解析拒绝；JSON 文件通过标准库解析，`git diff --check` 通过。

- 2026-08-24：修正 `docs/component-analysis/shadow-dev-action-direction-comparison-jwst0716.md` 的旧方案口径：旧方案改为 116 个历史轮次中 residual_analysis.py 原始流程写入的 `all_bands_comparison_component_analysis_*.md` 最终调整决策，而不是 numeric-only shadow baseline。报告逐轮并列旧决策原文摘要、辅助动作标签、新 VLM shadow 最终动作、当前成分和专家终态；原始来源覆盖为 108 轮可解析、6 轮缺失、2 轮多文件冲突、0 轮决策段无法解析，均明确标注而不以其他 artifact 替代。
- 2026-08-25：扩充 `docs/component-analysis/jwst0716-vlm-scientist-discussion-brief.md`，将成分分析从开始改造到 JWST0716 116 轮 shadow 的进展、当前可确认与不可确认的结论、旧新方案比较边界、盲法动作审查和 A/B refit 评价框架统一整理到科学家讨论入口；保留已裁定的中心语义方案 B、candidate overlay、diffraction_psf 三个案例和 Edge-on Disk 暂缓范围。未修改正式 workflow、规则阈值或科学裁决。
- 2026-08-25：按讨论入口精简 `jwst0716-vlm-scientist-discussion-brief.md`，删除独立的“已裁定并已实现”章节，将 `diffraction_psf` 的定义、Bar 衍射冲突以及 band、panel、region 的含义并入科学家裁定问题；后续章节编号顺延。未修改代码、正式 workflow、规则阈值或科学裁决。
- 2026-08-26：完成动作可达性审计。确认 116 轮均为 proposal-only：policy 前为 32 轮 `PROPOSE_ADD`、16 轮 `KEEP_AND_CONTINUE`、68 轮 `INCONCLUSIVE`，policy 后压缩为 48 轮 `PROPOSE_ADD`、68 轮 `KEEP_AND_CONTINUE`；当时 `PROPOSE_REMOVE`、`REFIT_PARAMETERS`、`CONVERGED` 不可达，batch runner 未执行 `EVALUATE_REFIT`，且对象级 PolicyState 未跨轮保持。审计据此冻结 action contract、证据前置、状态机、schema v1.1、文件级任务、测试矩阵和 v2 重跑口径；本轮只修改文档，未修改代码、阈值或正式 workflow。
- 2026-08-26：按已确认的决策逻辑补齐顺序完成 decision artifact v1.1 与兼容 loader、参数／约束／拟合健康／绝对残差事实、全规则候选收集、`REFIT_PARAMETERS`、`PROPOSE_REMOVE`、`CONVERGED`、operation-aware `EVALUATE_REFIT`、raw／resolved policy 状态和对象级 shadow runner；聚焦回归 130 passed，未修改科学阈值、Edge-on Disk、`diffraction_psf` 或正式 workflow。
- 2026-08-26：v2 numeric-only proposal shadow 按对象分成 4 个 shard 完成 116/116、失败 0；随后在获得历史 FITS 派生数据和 prompt 发往 `.env` 外部 provider 的明确授权后，完整 VLM v2 同样完成 116/116、失败 0。完整 VLM 解析为 96 轮 `OK`、20 轮 `PARSE_FAILED`；最终 action 为 `REFIT_PARAMETERS` 91、`PROPOSE_ADD` 20、`KEEP_AND_CONTINUE` 2、`CONVERGED` 2、`NONE` 1，raw action、resolved action、candidate actions、workflow status 和对象级 PolicyState 均保留。结果位于 `docs/component-analysis/shadow-dev-results-jwst0716-vlm-actions-v2.json`，动作覆盖报告位于 `docs/component-analysis/shadow-dev-action-coverage-jwst0716-v2.md`；未执行 GALFIT、`EVALUATE_REFIT` 或正式 workflow 锁定，未覆盖 2026-08-20 历史结果。
- 2026-08-26：全量测试结果为 175 passed、1 skipped、15 failed；15 项失败均为既有非本任务集成边界（缺少可选 claude-agent-sdk、旧 comparison PNG 返回值断言和旧 workflow 日志断言），本轮成分分析聚焦回归为 130 passed。
- 2026-08-26：结合 JWST0716 科学家讨论回填和 numeric-only／完整 VLM v2 shadow，新增 `docs/component-analysis/workflow-redesign-execution-plan.md`，分别规划单波段 `workflow_galfit.md`＋`AGENTS.md` 与多波段 `workflow_galfits.md`＋`CLAUDE.md` 的结构化 action、对象级 PolicyState、`EVALUATE_REFIT`、verifier 和受控 pilot 接入。计划明确 20 个 `PARSE_FAILED` 为 JSON 格式失败后的数值降级边界，`PROPOSE_REMOVE` 在真实局部残差事实和 A/B pilot 前保持 review-only；本轮未修改正式 workflow、代码或科学阈值，待确认项已列入计划第十一节。
- 2026-08-26：根据 workflow 完成性和 Agent 能力利用问题修订 `workflow-redesign-execution-plan.md`：明确 `STOPPED_NEEDS_REVIEW` 只停止自动成分决策循环；已有有效拟合结果时继续完成报告及多波段 SED／joint 收尾，区分 `COMPLETED_WITH_REVIEW` 与 `FAILED_NEEDS_REVIEW`。新增 bounded agentic policy，允许 VLM／Agent 在规则生成的合法候选内排序、解释冲突和参与 `EVALUATE_REFIT`，但不允许绕过确定性 veto 或直接修改配置。本轮仍未修改正式 workflow、代码或科学阈值。
- 2026-08-27：确认第 7 项：`origin/main` 已包含 Lens 更新，当前 `codex/component-analysis-redesign` 分支已包含对应提交；多波段 workflow 本次允许自动提出和执行 Lens，计划已同步增加 Lens 的 action、refit 和 verifier 测试要求。未执行分支合并，未修改 `main`、正式 workflow、代码或科学阈值。
- 2026-08-27：按 `docs/component-analysis/workflow-redesign-execution-plan.md` 实现结构化 workflow bridge：新增单／多波段显式 round manifest、raw／resolved decision 绑定、候选与 action preflight、单轮单动作配置适配器、对象级 `PolicyState`、持久化 `workflow_evaluate_refit`、fit lifecycle 和完整 action summary。实际拟合继续由现有 `run_galfit`、`run_galfits_image_fitting`、SED／joint MCP 工具执行；bridge 仅在 `COMPONENT_ANALYSIS_WORKFLOW_PILOT=1` 且对应 GALFIT／GalfitS 已配置时注册，未接管默认 workflow。单波段和多波段 prompt、`AGENTS.md`、`CLAUDE.md`、多波段 component specification 与 verifier 已同步；Edge-on、现有阈值、`diffraction_psf` 和历史 v2 结果未被改写。
- 2026-08-27：结构化 workflow 聚焦回归 `/home/www/ENTER/envs/galfit/bin/python -m pytest -q tests/test_workflow_bridge.py tests/test_policy.py tests/test_schemas.py tests/test_shadow.py`：82 passed，存在既有 `asyncio_mode` 配置警告；相关 Python 模块 `py_compile` 通过。最终全量回归为 193 passed、1 skipped、15 failed；15 项失败均为既有可选 `claude-agent-sdk`、comparison PNG 返回值和 workflow 日志断言边界，本次新增 workflow 测试没有失败。
- 2026-08-27：workflow v3 numeric-only shadow 完成 116/116、失败 0，VLM 未调用且所有条目保留 numeric fallback 的 review 标志；完整 VLM v3 经当前外部 provider 完成 116/116、失败 0，解析为 98 `OK`、18 `PARSE_FAILED`，共 137 次 provider 尝试（21 次 retry）。numeric-only 与 VLM v3 resolved action 仅 2/116 轮不同，不能据此宣称新 workflow 优于旧 workflow，也未执行真实 GALFIT／GalfitS、A/B refit 或正式 best-round 锁定。新结果分别位于 `docs/component-analysis/shadow-dev-results-jwst0716-workflow-v3-numeric-only.json` 与 `docs/component-analysis/shadow-dev-results-jwst0716-workflow-v3-vlm.json`，v2 checksum 保持不变。
- 2026-08-27：完成首轮 workflow bridge 接管审查，并将后续唯一实施入口写入 `docs/component-analysis/workflow-redesign-execution-plan.md` 第十三节。计划新增正式 proposal／resolve service、无歧义状态机、完整 mode-specific action compiler、约束保留、确定性 refit comparator、程序化 verifier、稳定 MCP 生命周期接口和阶段 A～G 验收门；同时明确核心 workflow 面向任意合规 MCP 客户端，`AGENTS.md`、`CLAUDE.md` 与 Claude Code subagent 只作为客户端适配层。本轮只更新方案和路线图，未修改代码、科学阈值、开关或历史结果。
- 2026-08-27：删除已执行完毕且状态过期的 `docs/component-analysis/decision-logic-completion-plan.md`，其关键审计结论和实施结果保留在本路线图；后续工程只以 `docs/component-analysis/workflow-redesign-execution-plan.md` 第十三节为执行入口。
- 2026-08-27：完成 workflow 接管阶段 A：新增 `agent_recommendation` schema 和客户端无关的 `workflow_capabilities` MCP 能力发现契约，明确 Agent 只能在服务端候选集合内排序、不得写配置或绕过 policy；新增无客户端 prompt 依赖的 contract 测试，`tests/test_workflow_lifecycle_contract.py tests/test_workflow_bridge.py tests/test_schemas.py` 为 61 passed。阶段 B 待开始；未修改科学阈值、`.env`、默认开关或历史结果。
- 2026-08-27：完成 workflow 接管阶段 B：新增共享 `decision_service`，正式 proposal／resolve 入口消费真实单／多波段 workflow manifest，shadow runner 改为复用同一 service；补齐 result HDU、band order、sigma／mask／PSF、参数文件和 provider retry／raw response 契约，缺失 sigma 明确降为 `UNAVAILABLE`，不以单位噪声替代。`tests/test_workflow_decision_service.py tests/test_artifact_adapter.py tests/test_shadow.py tests/test_workflow_bridge.py tests/test_workflow_lifecycle_contract.py tests/test_schemas.py` 为 73 passed。阶段 C 待开始；首轮 bridge 仍未通过正式接管验收，未执行真实单／多波段 pilot，未修改科学阈值、`.env`、默认开关或历史结果。
- 2026-08-27：完成 workflow 接管阶段 C：修复 `STOPPED_NEEDS_REVIEW`、`KEEP_AND_CONTINUE`、`CONVERGED` 和无 action 的拟合调用边界；为对象 `PolicyState` 增加 `workflow-state@v1`、event history 和 event ID 幂等，state 与 action config 使用临时文件及原子替换；新增幂等与 `CONVERGED` preflight 回归，`tests/test_workflow_decision_service.py tests/test_workflow_bridge.py tests/test_policy.py` 为 40 passed。阶段 D 待开始；首轮 bridge 仍未通过正式接管验收，未执行真实单／多波段 pilot，未修改科学阈值、`.env`、默认开关或历史结果。
- 2026-08-27：完成 workflow 接管阶段 D：补齐单／多波段 `SET_INITIAL`、`SET_BOUNDS`、`FIX_VALUE`、`FREE_VALUE`、`LINK_CENTER` 和 `REMOVE_NONREQUIRED_CONSTRAINT`；保留并合并既有 `.cons`／`.constrain`，多条中心关系合并到单一 `Update_Constraints`；多波段新增合法宿主 P、独立 Companion G、AGN N，Fourier m=1 原位改造 Disk，Lens 校验 `Re_disk > Re_lens > Re_bar`、`n < 0.5`、`q > 0.5`。配置生成后经过现有 feedme／lyric parser 检查，`tests/test_workflow_bridge.py tests/test_workflow_decision_service.py tests/test_policy.py tests/test_schemas.py` 为 90 passed，存在既有 `asyncio_mode` 配置警告。阶段 E 待开始；首轮 bridge 仍未通过正式接管验收，未执行真实单／多波段 pilot，未修改科学阈值、`.env`、默认开关或历史结果。
- 2026-08-27：完成 workflow 接管阶段 E：新增 workflow-fit-artifact v1 规范化和确定性 refit comparator；只接受现有 MCP 工具返回的真实 config、result、summary、comparison 与 band 引用，`workplace`-only 返回保持 incomplete。比较依据来自 FITS residual、参数解析、summary BIC 和 mode／band／数据条件；新增 `workflow_complete_candidate`，接受候选或保留审计产物并恢复 baseline。阶段 E 聚焦回归 `tests/test_workflow_bridge.py tests/test_workflow_decision_service.py tests/test_policy.py tests/test_schemas.py` 为 94 passed，存在既有 `asyncio_mode` 配置警告。首轮 bridge 仍未通过正式接管验收，未执行真实单／多波段 pilot，未修改科学阈值、`.env`、默认开关或历史结果。
- 2026-08-27：完成 workflow 接管阶段 F：新增 `verifier-assessment@v1`、`workflow-verifier@v1` 和共享只读 verifier；程序化检查 artifact、参数、约束、可比残差指标和 lifecycle，客户端 assessment 记录 client／model／prompt version 与证据；新增 `workflow_verify_best_round` 和只接受 verifier artifact 引用的 `workflow_lock_best_round`，禁止直接传入字符串 `PASS`。Claude Code verifier 已降为共享工具包装层，`AGENTS.md`、`CLAUDE.md`、两个 workflow prompt 和根 README 已改为跨客户端逻辑工具契约。阶段 F 回归 `tests/test_workflow_bridge.py tests/test_workflow_decision_service.py tests/test_workflow_lifecycle_contract.py tests/test_policy.py tests/test_schemas.py` 为 99 passed，存在既有 `asyncio_mode` 配置警告。阶段 G 待开始；首轮 bridge 仍未通过正式接管验收，尚未执行真实单／多波段 pilot，未修改科学阈值、`.env`、默认开关或历史结果。
- 2026-08-31：阶段 G 聚焦回归 `tests/test_workflow_lifecycle_contract.py tests/test_workflow_bridge.py tests/test_workflow_decision_service.py` 为 44 passed；实际 MCP server 在 `COMPONENT_ANALYSIS_WORKFLOW_PILOT=1` 下注册 10 个结构化 workflow 工具，Codex `0.149.0` 和 Claude Code `2.1.206` 客户端命令可用；全量回归为 `232 passed、5 skipped、0 failed`，仅有既有 `asyncio_mode`、Astropy 和 Photutils 警告。
- 2026-08-31：完成真实单波段 pilot（`NGC1097`），使用现有 `run_galfit` MCP 工具完成 baseline／candidate 拟合、确定性 refit evaluation、状态文件恢复、lifecycle 收尾和程序化 verifier；candidate 与 baseline 的残差分数相同，判定为可接受的 tie，verifier 五项硬门 `PASS`，best round `LOCKED`。artifact 保存在 `/tmp/workflow-pilot-single-Jbu0Vq/`。
- 2026-08-31：完成真实多波段 pilot（`obj170`），使用现有 `run_galfits_image_fitting` MCP 工具完成 7 波段 baseline／candidate 拟合、band order 校验、确定性 refit evaluation、状态恢复、候选拒绝回滚、lifecycle 收尾和程序化 verifier；candidate 拟合成功但 residual outcome 为 `worse`、BIC gain 为 `-213825.0`，因此判定 `REJECTED` 并恢复 baseline；verifier 五项硬门 `PASS`，best round `LOCKED`。第一次 GPU 执行因 CUDA OOM 失败，使用相同 MCP 拟合入口切换 JAX CPU 后完成；artifact 保存在 `/tmp/workflow-pilot-multi-*` 和 `/tmp/output/20260831_103209_workflow-pilot-multi-obj_170/`。
- 2026-08-31：复核 v2 shadow artifact 仍为 `116 samples、failures=0`，action 汇总和历史文件未被改写；本次仅新增多波段 result 按显式 band 名排序和既有 GalfitS 单 profile `Ga2) a` 的 validator 兼容回归，不修改科学阈值、`.env`、provider 凭据、默认启用开关、Edge-on Disk、`diffraction_psf` 或历史结果。阶段 G 工程验收完成；正式默认 workflow 接管及 `PROPOSE_REMOVE` 自动删除仍待单独授权和科学家评审。
- 2026-08-31：检查 Luna 阶段 A～G 产物和旧 `batch_analyze_galfits_mcp.sh`，确认阶段 G 已完成但 JWST0831 批量接管尚未实现；确认 `/home/www/2026/GALFITS_examples/jwst0831` 有 34 个星系目录及 `sigma/`，根目录 `.env` 已设置 `VLM_TIMING_LOG=1`。新增执行计划第十四节，冻结 H～M 顺序、timing adapter、批量 runner、canary／pilot 和待确认事项；本轮未修改代码、`.env`、凭据、默认开关或历史结果。
- 2026-08-31：阶段 H 完成：新增 `src/tools/workflow_batch_runner.py`、批次 manifest／对象 state／结构化 summary 和 `workflow-batch-manifest@v1` contract；`batch_analyze_galfits_mcp.sh` 原位改为 Python runner 薄启动包装层。阶段 H 回归为 `48 passed`，`bash -n` 和 `git diff --check` 通过；对真实 JWST0831 dry-run 发现 34 个对象、34 个 `READY`、0 个 `PREFLIGHT_FAILED`，运行目录为 `/tmp/jwst0831-workflow-h-QT8e1j`。JWST0831 外部 VLM provider 授权已确认；未执行拟合，未修改 `.env`、凭据、默认开关或历史结果。
- 2026-08-31：阶段 I 完成：`workflow_batch_runner.py` 通过 MCP stdio client 接入 `workflow_capabilities`、proposal／resolve、全 action preflight、候选 fit、refit completion、verifier、lock 和 Image → SED → Image-SED handoff；mock end-to-end、resume／idempotency 和既有 workflow 回归共 `115 passed`，真实 MCP capability discovery 返回 pilot enabled、single／multi-band mode 及现有拟合入口。未执行 JWST0831 拟合，未修改 `.env`、凭据、默认开关或历史结果。
- 2026-08-31：阶段 J 完成：新增结构化 VLM timing adapter，记录每次 provider attempt 的开始／结束时间、耗时、解析状态和 fallback 状态，并在 `VLM_TIMING_LOG=1` 时带文件锁追加到对象 `timing_log.md`；同步扩展 capability discovery 和 VLM provider schema，补充 GalfitS SED 的 `new_lyric_file` 结构化返回。阶段 J 回归为 `104 passed`，MCP server 重启后返回 `timing_log_enabled=true`；未调用真实 provider，未执行 JWST0831 拟合，未修改 `.env`、凭据、默认开关或历史结果。
- 2026-09-01：阶段 K 完成 JWST0831 单对象 `104` 多波段 canary：真实 Image、SED、Image-SED 均通过现有 MCP 工具完成，baseline、round manifest、raw／resolved decision、candidate fit／refit evidence、PolicyState、timing_log 和 terminal state 均可定位；最终状态为 `COMPLETED_WITH_REVIEW`，原因是决策轮预算结束，未出现 `CONVERGED`，因此未生成 verifier artifact、未执行 lock。canary 首次 SED 暴露的既有内部 `python` 解释器路径问题已修复为沿用 MCP 当前解释器；同时修复 CLI resume 覆盖、RUNNING／终态 downstream 恢复和状态归一，相关回归为 `21 passed`。未评价科学拟合优劣，未修改 `.env`、凭据、默认开关或历史结果；artifact 根目录为 `/tmp/jwst0831-canary-kLZEH9/`，新增目标产物位于 JWST0831 对象 `104` 的独立 output 目录。
- 2026-09-01：按最新授权将阶段 L 从 34 对象全量执行收缩为 manifest 前三个对象 `104`、`1071`、`1118`，并先更新第十四节第 9 项。原全量进程在 `104`、`1071` 完成且 `1118` 进入 SED 收尾时主动中止，`1182` 仍为 `NOT_STARTED`；artifact 根目录 `/tmp/jwst0831-pilot-full-xo2S8L/` 保留用于限定对象续跑和审计。本轮后续不会自动进入第 4 个对象，未修改 `.env`、凭据、默认开关或历史结果。
- 2026-09-01：阶段 L 的 JWST0831 三对象实际拟合完成：`104`、`1071`、`1118` 的 Image／SED／Image-SED 各 3 个 `COMPLETED`；6 次 provider attempt 均为 `parse_status=OK`、`fallback_status=OK`，三个 `timing_log.md` 均存在，未发生 GPU OOM 或 CPU fallback。新增续跑对象筛选修复，聚焦回归 `19 passed`；限定 `--resume --object-id 1118` 重复运行前后输出目录计数保持 `27／6／4`，`1182` 始终 `NOT_STARTED` 且输出计数为 0。最终审计发现根级 PolicyState 仍为 `FIT_AVAILABLE／PENDING_VERIFIER`，没有随 batch `COMPLETED_WITH_REVIEW` 同步，故阶段 L 暂不标记完成；未评价科学优劣，未修改 `.env`、凭据、默认开关或历史结果，artifact 根目录为 `/tmp/jwst0831-pilot-full-xo2S8L/`。
- 2026-09-01：阶段 L 复验完成：runner 对 review terminal 新增共享 lifecycle 终态写入，并在限定续跑时修复旧的 terminal PolicyState；相关 runner／workflow bridge 回归 `55 passed`。`104`、`1071`、`1118` 的 batch state 与 PolicyState 现均一致为 `COMPLETED_WITH_REVIEW／UNLOCKED／needs_review=true`，`pending_action=null`，各自存在 `terminal_lifecycle.json`；输出目录计数仍为 `27／6／4`，`1182` 仍为 `NOT_STARTED` 且输出计数为 0。阶段 L 正式完成，未重新拟合、未评价科学优劣，未修改 `.env`、凭据、默认开关或历史结果。
- 2026-09-01：阶段 M 第一轮全量回归 `256 passed, 5 skipped`，新脚本、报告、shell 检查和真实 dry-run 均已完成；最终 schema 审计发现 runner 把 MCP 返回的 `lifecycle_file`、`fit_valid` 便捷字段覆盖写回 lifecycle artifact，导致 `additionalProperties=false` 校验失败。PolicyState 和拟合内容正确，但阶段 L～M 恢复为复验中，完成归一化修复、三个对象真实 artifact schema 审计和修复后全量回归前不得声明完成。
- 2026-09-01：阶段 M 最终完成：runner 在 lifecycle 落盘前剥离 MCP 返回层的 `lifecycle_file`、`fit_valid` 元数据并执行 schema 校验；限定续跑已归一化三个对象的既有文件，共 12 个 baseline／candidate／terminal lifecycle artifact 全部通过 `workflow_lifecycle` schema。修复后聚焦回归 `56 passed`，最终全量回归 `257 passed, 5 skipped`；新脚本、报告、shell 检查、`git diff --check` 和真实 dry-run 均通过。当前只声明三对象 pilot 可运行；其余 31 个对象未执行，默认接管、`.env`、凭据和 `PROPOSE_REMOVE` 自动删除均未开启或修改。
- 2026-09-02：根据科学口径修正 `DISK_N_NOT_FIXED`：首轮未标注单个 Sersic 不再默认归类为 Disk；确认 Disk 后固定 `n=1` 属于规范合规动作，不因残差、BIC 或 reduced chi-square 变差单独回滚，但仍要求拟合收敛和参数物理有效。多波段 refit artifact 改为按 band token／manifest 显式路径配对，并保存 reduced chi-square 对比；新增 workflow 架构说明和 `gpt-image-2` 架构图。逐轮人类总结 renderer、Round 0 detect artifact 和 output 下的持久化目录仍待后续执行方案实施。
- 待办补充：旧 timestamped 拟合结果的删除仍需小鱼儿确认精确清单；剩余 31 个 JWST0831 对象只能在新 workflow 报告确认后由用户脚本运行，当前不得启动。
- 2026-09-02：本轮代码和文档修正后全量回归 `260 passed, 5 skipped, 3 warnings`，耗时约 8 分 39 秒；新增的 Disk n=1 强制动作、首轮单 Sersic 未分类和多波段 band/result 映射测试通过。warning 仍为既有 `asyncio_mode` 配置提示及两条 Photutils 合成图像提示。
- 2026-09-03：确认 JWST0831 的多波段入口必须严格使用 `run_galfits_image_fitting` → `run_galfits_sed_fitting` → `run_galfits_image_sed_fitting`；`run_galfit` 不用于对象 `104`、`1071`、`1118`。后续先对已有三对象 artifact 做离线回填，再用新 run ID 重新建立三对象 baseline；旧对象 `output/` 下已盘点的 timestamped 拟合中间／结果目录获授权在回填审计后删除，基础输入和 `/tmp` 历史 artifact 暂保留。renderer 采用本地确定性实现，Luna 仅负责客户端调度和可选解释，不参与机器动作或 refit 判定。执行方案详见第十六节。
- 2026-09-03：根据最新讨论，旧三对象 Image 阶段未达到多轮收敛和最佳轮锁定，不再进行正式 artifact 回填；旧 artifact 仅作为 renderer 离线 fixture。JWST0831 新 workflow 必须先自动执行多轮 `run_galfits_image_fitting`，只有 Image `CONVERGED`、verifier `PASS` 且 best round `LOCKED` 后，才允许调用 `run_galfits_sed_fitting` 和 `run_galfits_image_sed_fitting`；未收敛、轮次耗尽、工具失败或 verifier 非 `PASS` 时下游均为 `NOT_RUN`。
- 2026-09-03：按执行方案第十七节完成阶段一实现：新增本地确定性 `workflow_summary_renderer.py`，每个 Image round 生成 `summaries/round_<id>_component_analysis.md`、对象级 `working_note.md` 和最终 `analysis_report_obj<ID>.md`，缺失或 schema 失败字段写 `unavailable`／“未采集”；runner 显式调用 `workflow_evaluate_refit`，并固化 Image `CONVERGED` → verifier `PASS`／`lockable=true` → best round `LOCKED` 后才允许 SED／Image-SED 的门控，其他路径下游均为 `NOT_RUN`。新增 JWST0831 `104`、`1071`、`1118` 白名单、round artifact checksum／索引和对象／批次容量报告。renderer、runner、bridge 聚焦回归 61 passed，全量回归 264 passed、5 skipped、3 warnings。未启动 JWST0831 真实拟合、旧 artifact 回填或任何删除；本条工程验收不代表科学上确认全局最优。
- 2026-09-03：完成旧三对象 renderer fixture 离线检查：`104`、`1071`、`1118` 各 2 个历史 round 的 manifest、proposal、decision、lifecycle 均通过对应 schema；确定性 renderer 输出逐轮 summary、Working Note 和最终报告到 `/tmp/jwst0831-renderer-fixture-NHoxGl/`，并显式标记 `fixture_only=true`、`historical_pre_fix=true`。未写回历史 artifact，未生成新 scientific decision／lifecycle，未执行真实拟合或删除。
- 2026-09-03：补齐逐轮摘要的持久化 `artifact_index.json` 引用：artifact 复制、checksum／索引写入完成后，runner 再调用确定性 renderer 重渲染该 round，确保人类可读摘要中的索引路径真实存在；语法检查、workflow 聚焦回归 `61 passed`，全量回归 `264 passed、5 skipped、3 warnings`。
- 2026-09-03：增加 runner 侧多波段候选入口校验，异常 `action_config` 不得将候选拟合切换到 `run_galfit` 或其他工具；候选入口检查及 renderer 回归 `23 passed`，未启动真实 JWST0831 拟合。
- 2026-09-03：同步更新 `workflow_batch_runner.py --help` 的模块说明，使其反映当前完整 MCP workflow coordinator 和 Image→SED handoff gate，而不是已过时的 Stage H manifest-only 描述。
- 2026-09-03：修正 `batch_analyze_galfits_workflow_pilot.sh --help` 中误列未授权对象 `1182` 的示例，改为白名单对象 `104`；未执行任何拟合。
- 2026-09-03：修复 `KEEP_AND_CONTINUE` 被误判为终止的问题，Image workflow 现在会递增 `round_index` 并继续下一轮；新增多轮推进 contract test，`tests/test_workflow_batch_runner.py` 为 `22 passed`。此前误停的 `/tmp/jwst0831-v2-jsdknz` 保留为失败 run 审计，不作为新 baseline。
- 2026-09-03：三对象新独立 run `jwst0831-20260903T105600Z` 实际执行完成：`104`、`1071`、`1118` 各 10 个 Image round，共 30 轮；均因 Image 轮次上限停止，最终为 `COMPLETED_WITH_REVIEW`、`FIT_AVAILABLE`、`UNLOCKED`、`needs_review=true`、`sed_joint_eligible=false`，SED／Image-SED 均为 `NOT_RUN`。持久化 artifact 共 276 个文件、约 92.2 MiB，三对象各有 10 个 round index、Working Note 和最终报告；未执行其余 31 个对象，未删除旧结果，不代表科学上确认全局最优。
- 2026-09-03：生成旧三对象 output timestamped 结果目录的删除前审计清单 `/tmp/jwst0831-old-output-inventory-20260903.txt`：37 个目录、585 个文件、314296232 bytes，逐文件包含字节数和 SHA-256；清单已生成但未执行删除，等待确认。
- 2026-09-03：修复后最终全量回归为 `265 passed、5 skipped、3 warnings`（约 10 分 30 秒）；新增 `KEEP_AND_CONTINUE` 多轮推进测试纳入回归，三对象实际 run 的逐 round schema 校验通过。
- 2026-09-04：收到删除确认后，按 `/tmp/jwst0831-old-output-inventory-20260903.txt` 逐文件复核 37 个旧 timestamped output 目录的 585 个文件，314296232 bytes 和全部 SHA-256 均一致；随后仅删除清单中的三个对象 `output/` 子目录，删除日志为 `/tmp/jwst0831-old-output-deletion-20260904.txt`。删除后旧清单目标为 0 个残留；三对象新 run `jwst0831-20260903T105600Z` 的 `workflow/`、新 `20260903_*` 结果目录、科学输入和基础 lyric 均保留，其余 31 个对象未执行。本次删除不代表科学上确认全局最优。
- 2026-09-04：根据新 sigma 文件位置，补齐 JWST0831 `104`、`1071`、`1118` 基础 lyric 的 7 波段 `Ia3`～`Ig3` sigma 路径；未修改历史 `_iter*.lyric`。执行方案新增第十八节，明确 `single_sersic` 与已确认成分的状态语义、`resolved_decision` 与候选动作矩阵，以及 N1～N5 的真实多轮闭环改造和验收顺序。当前仍不得启动剩余 31 个对象；真实多轮闭环尚未验收。
- 2026-09-07：根据小鱼儿确认的改造方案，将 `docs/component-analysis/workflow-redesign-execution-plan.md` 重写为 288 行的当前 Luna 执行版，集中冻结所有动作类型、`KEEP_AND_CONTINUE`／`STOPPED_NEEDS_REVIEW` 语义、`PROMOTE_SINGLE_SERSIC_TO_DISK`、`PROPOSE_REPLACE`、`PROPOSE_REMOVE` 的 lyric 编译契约、runner 顺序、测试和 104 canary 验收门。旧版 862 行内容保留在同名 `.orig` 快照中，不作为执行入口；真实多轮 Image 闭环仍未验收。
- 2026-09-07：完成 N0～N5 workflow 改造并通过全量回归 `271 passed、5 skipped、3 warnings`。新增 promotion／replace／remove lyric 编译、Disk n=1 强制评价、行内 `semantic_label` profile 回流、动作＋基线 fingerprint 幂等保护，以及终止 `action=null` 的确定性 renderer 处理。仅执行 JWST0831 对象 `104` 的 v5 canary：baseline → `PROMOTE_SINGLE_SERSIC_TO_DISK`（ACCEPTED）→ `PROPOSE_ADD companion`（REJECTED）→ 重复候选拒绝后 `STOPPED_NEEDS_REVIEW`；三个不同 Image lyric／result FITS 均登记，每个候选均调用 `workflow_evaluate_refit`，最终 `COMPLETED_WITH_REVIEW`、`FIT_AVAILABLE`、`UNLOCKED`、`needs_review=true`，SED／Image-SED 均为 `NOT_RUN`。未执行 `1071`、`1118` 或其余 31 个对象；该工程验收不代表科学上确认全局最优。
- 2026-09-07：复核 Luna 104 canary 的停止证据：Round 0 的 Disk promotion 在拟合收敛且参数物理有效后接受；Round 1 companion 虽残差改善且收敛，但 `obj0.re` 命中参数边界、`BIC_gain=-163.07`，由综合 refit 规则拒绝；相同动作和 baseline 再次出现时转 `STOPPED_NEEDS_REVIEW`。因此最终 `FIT_AVAILABLE` 仅表示保留有效 Image 结果，`UNLOCKED` 表示尚未通过 verifier PASS／lock，不代表最佳轮次或科学最优。
- 2026-09-07：按授权删除 9 月 3 日三个对象的 12 个旧 output／workflow 目录，共 450 个文件、162595678 bytes；删除前逐文件 SHA-256 清单为 `/tmp/jwst0831-pre-20260907-output-inventory.txt`，清单自身 SHA-256 为 `9cbdfd97a155147560c29826c0d74544e993daec2332c394369c4d9b44194bb2`。所有 9 月 7 日结果、基础 lyric、science／sigma／mask／PSF 均保留；`1071`、`1118` 当日未执行。
- 2026-09-08：按授权清理对象 `104` 的 v1～v4 无效 canary：删除四个根级实际拟合目录、四个持久 workflow run 和四个 `/tmp` run，共 12 个精确目录、685 个文件；删除前路径及逐文件 SHA-256 清单为 `/tmp/jwst0831-invalid-canary-20260907-inventory.txt`，清单 checksum 为 `e3e7c861cdcd42bfb73d9a84838a22e49e243f4cc305d7b6c84b4fa29adea546`。v5 的实际拟合目录、持久 workflow 和 `/tmp` 审计根目录均已复核保留；删除不可直接恢复。
- 2026-09-08：完成 Luna v5 只读审计。确认 batch manifest 为 `use_vlm=false`、三轮 provider 均 `DISABLED`，故 VLM summary 的 `unavailable` 表示未调用；同时复现 Round 1 `candidate_lifecycle.json` 因顶层 `lifecycle_file`、`fit_valid` 违反 schema，导致 renderer 漏报已落盘的 refit evaluation。将 N7～N11 的 lifecycle／provider／renderer 修复、v5 离线 replay、全量回归和仅 `104` 的 VLM-enabled canary 顺序合并进 `docs/component-analysis/workflow-redesign-execution-plan.md` 第 13～16 节；本轮未执行新拟合，不表示科学上确认最优。
- 2026-09-08：读取并审计新的评测数据源 `/media/data/galfit_run_history` 与 `/media/data/galfits_run_history`，完成 `README.md`、多波段 `expert-final-labels.json` 和历史目录首轮盘点。确认单波段包含 Gadotti 100 个参数标签及 JWST F277W 25 个标签，多波段包含 32 个 `confirmed` 终态标签；多波段原始目录有 13 个批次、207 个对象实例和 1416 个 Image 轮次，存在跨批次重复、`_2` 变体和无标签对象。根据本次讨论将 `docs/component-analysis/evaluation-set-design.md` 更新为 v1，冻结 Image-only、对象白名单、专家终态仅作成分集合／语义映射、局部动作裁决、无未来信息 state、transition fingerprint 去重、对象级 split 和 `CORRECT + high` 核心集准入规则；尚未抽取正式评测集或执行新拟合。
- 2026-09-08：完成 N7～N8。lifecycle 落盘统一剥离 `lifecycle_file`／`fit_valid` 并按 schema 校验；renderer 增加 provider 状态、Round 0 四层语义、baseline／candidate 评价、Working Note、final report、run-scoped baseline 和 `run_index.json`。保留的 v5 artifact 完成只读离线 replay；聚焦测试和全量回归均通过，最终为 `271 passed、5 skipped、3 warnings`。
- 2026-09-08：完成仅对象 `104` 的全新 VLM canary，run 根目录为 `/tmp/jwst0831-n7-canary-104-20260908/`，持久化 workflow 为 `/home/www/2026/GALFITS_examples/jwst0831/104/output/workflow/jwst0831-20260908T034756Z/`。manifest 明确 `pilot_enabled=true`、`workflow_mode=multi-band`、`use_vlm=true`，实际链路为 baseline → `PROMOTE_SINGLE_SERSIC_TO_DISK`（ACCEPTED）→ `PROPOSE_ADD companion`（REJECTED）→ 相同候选被拒后 `STOPPED_NEEDS_REVIEW`，共 3 个 Image round；每个实际候选均生成新 lyric、调用 `run_galfits_image_fitting` 和 `workflow_evaluate_refit`。首轮 VLM provider 为 `USED／OK`，后两轮为 `PARSE_FAILED`，报告保留原始错误和 timing；最终 `COMPLETED_WITH_REVIEW`、`FIT_AVAILABLE`、`UNLOCKED`、`needs_review=true`，Image 未 `CONVERGED`，因此 SED／Image-SED 均为 `NOT_RUN`。未运行 `1071`、`1118` 或其余 31 个对象；该工程验收不表示科学上确认全局最优。
- 2026-09-08：补齐 runner 的 resume 配置指针：state 同时保存 run-scoped baseline lyric 和当前 accepted candidate lyric，中断恢复优先使用当前 lyric，避免 current FITS 与 baseline lyric 错配；`tests/test_workflow_batch_runner.py` 为 `23 passed`，语法检查通过。该修复只影响 resume 路径，不重新执行 104 canary。
- 2026-09-08：根据 104 VLM canary 的真实截断响应和决策链审计，更新 `docs/component-analysis/workflow-redesign-execution-plan.md` 第十七节为当前最高优先级：明确 VLM target 是 `1 central + numeric candidate_regions` 的动态集合，新增 provider metadata／稀疏输出／非重复 retry、Rules 逐规则 numeric fallback、`KEEP_AND_CONTINUE` 的 `COLLECT_EVIDENCE` 语义、离线回放和仅对象 `104` 的全新 VLM-enabled 闭环 canary；未启动真实拟合，未修改历史 artifact，也不代表科学上确认全局最优。
- 2026-09-08：完成 N12～N15：VLM provider 记录每次请求的 model／prompt version／attempt／响应字节数／finish reason／token usage／时间戳，采用动态稀疏 target 分批和非重复 retry，截断响应 fail-closed 并保留 raw response；Rules 在 VLM 解析失败时继续评估 numeric 规则，Policy 保存 VLM 与 numeric-only evidence views，并实现 `COLLECT_EVIDENCE`、fingerprint 幂等和五类模型动作的真实 refit 路径。离线 v5 replay 未写回历史 lifecycle、PolicyState、最佳轮次或 scientific decision；focused tests `116 passed`，全量回归 `275 passed、5 skipped、3 warnings`。
- 2026-09-08：完成 N16 仅对象 `104` 的全新 VLM-enabled canary，临时 run 为 `/tmp/jwst0831-n16-canary-104-xE3yEV/`，持久化 workflow 为 `/home/www/2026/GALFITS_examples/jwst0831/104/output/workflow/jwst0831-20260908T095838Z/`。manifest 确认 `pilot_enabled=true`、`workflow_mode=multi-band`、`use_vlm=true`；共 3 个 Image round，依次执行 `PROMOTE_SINGLE_SERSIC_TO_DISK`（ACCEPTED）和 `PROPOSE_ADD companion`（REJECTED），每个实际候选均生成新 lyric、调用 `run_galfits_image_fitting` 和 `workflow_evaluate_refit`。VLM 共 23 次 attempt，provider `USED` 3 轮，首轮截断 retry 后完成分批解析；最终因 Image 未达到 `CONVERGED → verifier PASS → LOCKED` 而为 `COMPLETED_WITH_REVIEW`、`FIT_AVAILABLE`、`UNLOCKED`，SED／Image-SED 均为 `NOT_RUN`。三轮 summary、Working Note、final report、provider timing、run_index 和 artifact index 均已落盘；未运行 `1071`、`1118` 或其余 31 个对象。本次通过的是工程链路验证，不表示科学上确认全局最优。
- 2026-09-08：补齐 N14 evidence view 可追溯性：Policy 对 VLM 成功和 numeric-only fallback 均显式保存 raw VLM、numeric-only 两层 view，确定性 renderer 另外展示 resolved view；对历史 schema-valid artifact 缺少 `evidence_views` 的情况，只从已校验 proposal／decision 重建报告视图，不修改动作或 scientific decision。104 canary 三轮 summary、Working Note 和 final report 已重渲染并确认三层视图存在；focused 回归 `23 passed`，最终全量回归 `276 passed、5 skipped、3 warnings`。
- 2026-09-08：完成 N12 provider metadata 收口：每个 VLM attempt、timing artifact 和 provider 顶层均记录 `model_id`、`prompt_version`、attempt、时间、响应字节数、finish reason 与 token usage；schema 和 renderer 同步显示 prompt version。聚焦回归 `71 passed、1 warning`，最终全量回归 `276 passed、5 skipped、3 warnings`。已完成的 104 canary 不重新执行；其落盘报告保持真实历史字段，后续新 run 将按新 schema 记录完整 metadata。
- 2026-09-09：根据 `docs/component-analysis/evaluation-set-design.md` 前 9 节批注完成评测方案初步修订（待确认版本）。将 `KEEP_AND_CONTINUE` 从历史下一步动作中移除，暂将 `CONVERGED` 按历史最佳轮次候选处理；补充利用下一轮及后续轮次拆分复合动作的规则、动作前必要性 `pre_action_necessity` 的可核验证据要求，以及 `EXPLORATORY`／`HARMFUL` 非核心审计池用途。因本数据集只用于评测，取消 calibration／validation／test split，改为 `benchmark`／`audit`／`excluded` 数据池；第 10 节执行方案已按上述口径同步更新。待确认事项包括 F277W `elliptical` 语义、是否建立独立终态选择任务、是否发布 audit 池和无法拆分复合动作的处理。
- 2026-09-09：根据小鱼儿确认的信息完成评测方案收口：JWST F277W 的 4 个 `elliptical` 标签（`obj1845`、`obj216`、`obj2185`、`obj2758`）固定映射为单波段 `single_sersic`，与 `expdisk` 模型的 `disk` 分开；不建立独立 `terminal_selection` 任务，`CONVERGED` 作为统一单步决策的终止输出。确认保留 `audit` artifact、无中间状态的复合动作排除核心集、Gadotti 参数暂不作为数值真值且不预设核心样本数量；第 10 节执行方案已同步为最终确认口径。
- 2026-09-09：将评测设计文档的最终口径进一步统一：`CONVERGED` 只作为统一单步决策中的终止输出，不建立 `terminal_selection` 样本、字段或独立指标；仅当历史最佳轮与专家终态一致且拟合、残差、参数健康均满足终止门槛时，`CONVERGED` 才标为 `CORRECT`。第 10 节产物名称、样本指纹和核心准入条件已同步，当前无待确认的设计项。
- 2026-09-09：新增领导审阅版 `docs/component-analysis/evaluation-set-plan-leadership-review.md`，将完整评测设计压缩为建设目标、数据基础、样本原则、七步执行流程、交付结果、范围控制和五项确认内容；详细技术规则仍以 `evaluation-set-design.md` 为实施依据。本轮未抽取评测集、未执行拟合。
