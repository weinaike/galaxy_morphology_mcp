# 训练数据生成方案 — 对比实验记录

## 1. 实验目标

验证不同提议策略（rule_based / expert_guided / vlm_generated）及关键配置参数对训练数据生成质量的影响，为批量生产选定最优配置。

### 核心对比维度

| 对比 | 实验组 | 验证问题 |
|------|--------|---------|
| 提议策略价值 | E1 vs E3 | VLM提议相比规则提议能提升多少成功率？ |
| 专家规则对比 | E2 vs E3 | VLM提议能否匹配或超越基于Gadotti GT的专家规则？ |
| 专家提示注入 | E3 vs E4 | 在VLM prompt中注入Gadotti专家参数是否提升效果？ |
| Token成本优化 | E3 vs E5 | VLM并发调用从4路减到2路，成功率损失多少？ |
| 深层轨迹探索 | E3 vs E6 | 关闭贪心+放宽早停能否产出更多深层训练数据？ |

## 2. 公共配置（所有实验保持一致）

所有实验均基于最新代码（commit 7b8efb8），确保可比性。

```python
# main_generate.py 公共配置
TEST_MODE = True                              # 测试模式，20个星系
USE_LLM_REWARD = True                         # VLM视觉打分（Gemini评估残差改善）
USE_PARAM_CHECK = True                        # 参数合理性审查（VLM同时检查物理参数）
VLM_REWARD_MODEL_NAME = "gemini-3.1-pro-preview"
VLM_PROPOSAL_MODEL_NAME = "gemini-3.1-pro-preview"
USE_HISTORY_FOR_VLM = True                    # 历史轮次注入VLM prompt
VLM_HISTORY_MAX_STEPS = 0                     # 0=注入全部历史
BEAM_TOP_K = 6                                # 每层保留chi2_nu最优的6个父节点
VLM_REWARD_IMAGE_MODE = "full"                # 完整comparison图(Original|Model|Residual|1D)
TARGET_BANDS = ["SDSS_gband"]                 # 单波段
FORCE_GREEDY = True                           # 贪心接受（仅接受改善的变体）
MAX_PATIENCE = 2                              # 连续N步零接受触发早停

# TEST_MODE 自动设置
max_steps = 6                                 # 最大搜索深度
num_variants = 16                             # 每步变体数
```

### SSIM阈值（galfit_env.py，所有实验一致）

| 条件 | 阈值 | 说明 |
|------|------|------|
| modify（纯调参） | 0.99999 | 最宽松，仅拦完全相同的残差 |
| Step 1 + add_* | 0.9999 | 中间 |
| 其他 add_*（Step 2+） | 0.9995 | 保守过滤 |

### 参数clamp规则（vlm_proposal.py `_normalize_component`，所有实验一致）

| 参数 | 范围 | 来源 |
|------|------|------|
| Re | ≥ 0.2 px | component_specification_galfit.md |
| q（通用） | [0.05, 1.0] | component_specification_galfit.md |
| q（bar） | [0.2, 0.4] | 教科书范围，统一适用 |
| n（非bar sersic） | [0.1, 8.0] | component_specification_galfit.md |
| n（bar） | VLM值 or 0.5, fix=0 | n=0.5固定，所有项目知识一致 |
| PA | [-180, 180] | GALFIT标准惯例 |

## 3. 实验矩阵

| 实验ID | PROPOSAL_STRATEGY | USE_EXPERT_HINT | NUM_CALLS | 其他差异 | 变量说明 | Log文件 |
|--------|-------------------|-----------------|-----------|---------|---------|---------|
| **E1** | `rule_based` | — | — | — | 基线：纯规则提议 | `exp_E1_rule_based.log` |
| **E2** | `expert_guided` | — | — | — | 基线：Gadotti专家规则 | `exp_E2_expert_guided.log` |
| **E3** | `vlm_generated` | False | 4 | — | VLM自主提议 | `new_20260625.log` (已有) |
| **E4** | `vlm_generated` | True | 4 | — | VLM + 专家提示注入 | `exp_E4_vlm_hint.log` |
| **E5** | `vlm_generated` | False | 2 | — | Token优化（并发减半） | `exp_E5_vlm_calls2.log` |
| **E6** | `vlm_generated` | False | 4 | FORCE_GREEDY=False, MAX_PATIENCE=4 | MH退火探索深层轨迹 | `exp_E6_mh_explore.log` |

### 各实验差异配置

**E1 — rule_based**
```python
PROPOSAL_STRATEGY = "rule_based"
USE_EXPERT_HINT_FOR_VLM = False
VLM_PROPOSAL_NUM_CALLS = 4
```

**E2 — expert_guided**
```python
PROPOSAL_STRATEGY = "expert_guided"
USE_EXPERT_HINT_FOR_VLM = False
VLM_PROPOSAL_NUM_CALLS = 4
```

**E3 — vlm_generated (已有 new_20260625.log)**
```python
PROPOSAL_STRATEGY = "vlm_generated"
USE_EXPERT_HINT_FOR_VLM = False
VLM_PROPOSAL_NUM_CALLS = 4
```

**E4 — vlm_generated + expert hint**
```python
PROPOSAL_STRATEGY = "vlm_generated"
USE_EXPERT_HINT_FOR_VLM = True
VLM_PROPOSAL_NUM_CALLS = 4
```

**E5 — vlm_generated + token优化**
```python
PROPOSAL_STRATEGY = "vlm_generated"
USE_EXPERT_HINT_FOR_VLM = False
VLM_PROPOSAL_NUM_CALLS = 2
```

**E6 — vlm_generated + MH退火探索（深层轨迹）**
```python
PROPOSAL_STRATEGY = "vlm_generated"
USE_EXPERT_HINT_FOR_VLM = False
VLM_PROPOSAL_NUM_CALLS = 4
FORCE_GREEDY = False    # 关闭贪心，启用 Metropolis-Hastings 退火接受
MAX_PATIENCE = 4        # 放宽早停耐心值（默认2→4）
```

> **E6 设计动机**：E3 在 Step 4+ 成功率断崖归零，原因是严格贪心（`force_greedy=True`）不允许中间步暂时变差。
> 真实星系拟合中，加新component（如bar）常常导致chi2先升后降，需要多轮调参才能收敛。
> E6 通过两个改动解决深层数据缺失问题：
>
> 1. **关闭贪心** (`FORCE_GREEDY=False`)：VLM判定 improvement=0 的变体仍有 ~13.5% 概率被接受
>    （Metropolis-Hastings: `P(accept) = exp(delta_r/T) = exp(-1/0.5) ≈ 0.135`），
>    允许pipeline走过"先变差再变好"的优化谷底，产出Step 4-6的训练样本。
> 2. **放宽早停** (`MAX_PATIENCE=4`)：允许连续4步无纯贪心接受才停止，给深层探索更多机会。
>
> **E3 vs E6 关注的核心指标**：
> - 深层（Step 4-6）是否有 accepted 节点
> - 训练数据深度分布是否更均匀
> - SFT/DPO 样本数量变化（预期：E6 > E3）
> - 成本变化（预期：E6 ≈ E3，VLM调用数相同，仅搜索深度更深导致略多GALFIT运行）

## 4. 实验结果

### 4.1 总体对比

| 指标 | E1 (rule) | E2 (expert) | E3 (vlm) | E4 (vlm+hint) | E5 (vlm×2) | E6 (MH探索) |
|------|-----------|-------------|----------|---------------|------------|-------------|
| 生成变体数 | | | 387 | | | |
| SSIM拦截数 | | | 104 | | | |
| SSIM拦截率 | | | 26.9% | | | |
| 有效评估数 | | | 266 | | | |
| Accepted数 | | | 75 | | | |
| **成功率** | | | **19.4%** | | | |
| 平均深度 | | | 1.20 | | | |
| 最大深度 | | | 3 | | | |
| 崩溃数 | | | 17 | | | |
| 耗时 | | | 4h12m | | | |
| 平均耗时/星系 | | | 757s | | | |

> 注：成功率 = Accepted / 有效评估数（过SSIM后进入reward的变体）
> E6的核心看点不是成功率，而是**平均深度**和**最大深度**

### 4.2 动作分布对比

| 动作 | E1 | E2 | E3 | E4 | E5 | E6 |
|------|----|----|----|----|-----|-----|
| add_disk 生成/通过/率 | | | 107/18/16.8% | | | |
| add_bar 生成/通过/率 | | | 17/0/0.0% | | | |
| add_psf 生成/通过/率 | | | 111/27/24.3% | | | |
| modify 生成/通过/率 | | | 152/30/19.7% | | | |

### 4.3 分步成功率

| Step | E1 | E2 | E3 | E4 | E5 | E6 |
|------|----|----|----|----|-----|-----|
| Step 1 | | | 23/72 (31.9%) | | | |
| Step 2 | | | 30/105 (28.6%) | | | |
| Step 3 | | | 22/93 (23.7%) | | | |
| Step 4 | | | 0/74 (0.0%) | | | |
| Step 5 | | | 0/43 (0.0%) | | | |
| Step 6 | | | — | | | |

### 4.4 Token消耗与成本

| 指标 | E1 | E2 | E3 | E4 | E5 | E6 |
|------|----|----|----|----|-----|-----|
| Proposal调用次数 | | | 536 | | | |
| Proposal Input tokens | | | 3,448,004 | | | |
| Proposal Output tokens | | | 1,781,879 | | | |
| Reward调用次数 | | | 266 | | | |
| Reward Input tokens | | | 2,518,071 | | | |
| Reward Output tokens | | | 476,150 | | | |
| **总Token** | | | **8,224,104** | | | |
| **估算成本($)** | | | **~$39** | | | |

> 成本按 gemini-3.1-pro-preview 定价：Input $2.00/1M, Output $12.00/1M（≤200k context）

### 4.5 全量估算（926个星系）

| 指标 | E1 | E2 | E3 | E4 | E5 | E6 |
|------|----|----|----|----|-----|-----|
| 预估耗时 | | | ~195h | | | |
| 预估成本($) | | | ~$1,800 | | | |
| 预估SFT样本数 | | | ~3,470 | | | |
| 预估DPO配对数 | | | TBD | | | |

## 5. 训练数据格式

### 5.1 SFT样本（监督微调正样本）

每条SFT样本 = 一个被accepted的树节点，训练模型学习"看到这样的残差图→应该输出这样的模型规格"。

```json
{
  "sample_id": "Plate0282_Fiber478_g__node_2_p0_v0",
  "galaxy_id": "SDSS_gband_Plate0282_MJD51658_Fiber478_g",
  "depth": 2,
  "coarse_label": "add_psf",
  "messages": [
    {
      "role": "system",
      "content": "你是一个星系形态拟合专家。根据当前拟合状态（参数表+残差图），输出下一轮完整模型规格..."
    },
    {
      "role": "user",
      "content": "## 当前拟合状态\n| 成分 | mag | Re | n | q | PA |\n|---|---|---|---|---|---|\n| Sersic_1 | 16.82 | 4.51 | 3.68 | 0.72 | 31.2 |\n\nchi2/nu=0.744 ...",
      "images": ["node_0_root_comparison.png"]
    },
    {
      "role": "assistant",
      "content": "```json\n{\"components\": [{\"role\":\"bulge\",\"model\":\"sersic\",\"mag\":17.5,\"re\":3.0,\"n\":4.0,\"q\":0.72,\"pa\":31.2,\"fix\":{\"n\":0}},{\"role\":\"nucleus\",\"model\":\"psf\",\"mag\":19.2,\"fix\":{}}],\"sky\":{\"value\":null,\"fix\":0},\"target\":\"添加PSF核点源拟合中心残差\",\"confidence\":0.90}\n```"
    }
  ],
  "metadata": {
    "parent_node_id": "node_0_root",
    "parent_chi2_nu": 0.744,
    "child_chi2_nu": 0.701,
    "reward_detail": {"improvement": 1, "confidence": 0.90, "param_plausible": true}
  }
}
```

**训练时**：input = system + user（含残差对比图），output = assistant（完整模型规格JSON）。

### 5.2 DPO样本（偏好对齐配对）

每条DPO pair = 同一父节点下的两个兄弟，一个被采纳（chosen）一个被拒绝（rejected），训练模型学习"区分好坏提议"。

```json
{
  "pair_id": "Plate0282__node_1_p0_v0_vs_v1",
  "galaxy_id": "SDSS_gband_Plate0282_MJD51658_Fiber478_g",
  "depth": 1,
  "prompt": [
    {"role": "system", "content": "你是一个星系形态拟合专家..."},
    {"role": "user", "content": "## 当前拟合状态\n...", "images": ["node_0_root_comparison.png"]}
  ],
  "chosen": "{\"components\":[{\"role\":\"bulge\",...},{\"role\":\"nucleus\",\"model\":\"psf\",\"mag\":19.2}],...}",
  "rejected": "{\"components\":[{\"role\":\"bulge\",...},{\"role\":\"disk\",\"model\":\"sersic\",\"mag\":16.5,\"re\":49.3}],...}",
  "metadata": {
    "winner_node_id": "node_1_p0_v0",
    "winner_coarse_label": "add_psf",
    "winner_chi2_nu": 0.701,
    "loser_node_id": "node_1_p0_v1",
    "loser_coarse_label": "add_disk",
    "loser_chi2_nu": 0.812,
    "loser_failure_reason": "param_runaway"
  }
}
```

**训练时**：input = prompt（相同的残差图+参数表），chosen = 好回答，rejected = 差回答。

### 5.3 抽取规则

**SFT**：遍历trajectory.json所有节点 → 跳过root → 跳过is_accepted=False → 跳过crash → 每个合格节点生成一条。

**DPO**：按parent_id分组 → winner池(is_accepted=True) × loser池(is_accepted=False且非crash) → 笛卡尔积配对 → 过滤confidence<0.7的loser。

## 6. 结论与最优配置

> 待所有实验完成后填写

### 最优配置推荐

```python
PROPOSAL_STRATEGY = "..."
USE_EXPERT_HINT_FOR_VLM = ...
VLM_PROPOSAL_NUM_CALLS = ...
```

### 推荐理由

> 待填写
